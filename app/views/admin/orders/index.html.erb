<h1>Список замовлень</h1>

<table table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th>Користувач</th>
      <th>Сума</th>
      <th>Дата створення</th>
      <th>Дії</th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.id %></td>
        <td><%= order.user.email if order.user %></td>
        <td><%= number_to_currency(order.total_price_cents) %></td>
        <td><%= l(order.created_at, format: :long) %></td>
        <td>
          <%= button_to 'Видалити', admin_order_path(order), method: :delete, data: { confirm: 'Ви впевнені?' }, class: 'btn btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

